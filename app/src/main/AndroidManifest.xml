<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.apps.id.rijks">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RijksApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".ui.activity.ActivitySplashScreen"
            android:screenOrientation="portrait"
            android:theme="@style/AppFullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.ActivityMain"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.activity.ActivityLogin"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.activity.ActivityRegister"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.activity.ActivityArtDetail"
            android:label="@string/title_detail"
            android:screenOrientation="portrait" />
    </application>

</manifest>